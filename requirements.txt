# Runtime dependencies for Sinclaire custom addons.
#
# Odoo itself and its own requirements are NOT pinned here - they are provided
# by the Odoo.sh / on-prem platform. List only extra packages that the
# sinclaire_* modules import directly.
